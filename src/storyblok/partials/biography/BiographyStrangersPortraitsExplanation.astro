---
import { storyblokEditable } from '#storyblok/helpers/storyblokEditable.ts';
import BiographyStrangersPortraitsExplanationVue from '#storyblok/partials/biography/BiographyStrangersPortraitsExplanation.vue';
import type { StoryblokBiographyStrangersPortraitsExplanation } from '#types/component-types-sb.js';

type Props = {
	blok: StoryblokBiographyStrangersPortraitsExplanation;
};

const { blok } = Astro.props;
---

<BiographyStrangersPortraitsExplanationVue {...storyblokEditable(blok, Astro.locals.isPreviewMode)} {...Astro.props} />
