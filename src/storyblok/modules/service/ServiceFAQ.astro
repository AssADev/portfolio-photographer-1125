---
import { storyblokEditable } from '#storyblok/helpers/storyblokEditable.ts';
import ServiceFAQVue from '#storyblok/modules/service/ServiceFAQ.vue';
import type { StoryblokServiceFAQ } from '#types/component-types-sb.js';

type Props = {
	blok: StoryblokServiceFAQ;
};

const { blok } = Astro.props;
---

<ServiceFAQVue {...storyblokEditable(blok, Astro.locals.isPreviewMode)} {...Astro.props} client:visible />
