---
import ProjectMinimapVue from '#storyblok/modules/project/ProjectMinimap.vue';
import type { StoryblokAsset } from '#types/component-types-sb.js';

type Props = {
	pictures: StoryblokAsset[];
};

const { pictures } = Astro.props;
---

<ProjectMinimapVue pictures={pictures} client:load />
