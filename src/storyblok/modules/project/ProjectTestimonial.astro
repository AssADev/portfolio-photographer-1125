---
import { storyblokEditable } from '#storyblok/helpers/storyblokEditable.ts';
import ProjectTestimonialVue from '#storyblok/modules/project/ProjectTestimonial.vue';
import type { StoryblokProjectTestimonial } from '#types/component-types-sb.js';

type Props = {
	blok: StoryblokProjectTestimonial;
};

const { blok } = Astro.props;
---

<ProjectTestimonialVue {...storyblokEditable(blok, Astro.locals.isPreviewMode)} {...Astro.props} client:visible />
