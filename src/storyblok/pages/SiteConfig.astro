---
// This block is needed for preview purposes :
import type { StoryblokSiteConfig } from '#types/component-types-sb.js';

type Props = {
	blok: StoryblokSiteConfig;
};
---

<Fragment>This is site config</Fragment>
