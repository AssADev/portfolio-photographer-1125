---
import type { StoryblokService, StoryblokServiceInformations } from '#types/component-types-sb.js';
import type { SbBlokData } from '@storyblok/astro';
import StoryblokComponent from '@storyblok/astro/StoryblokComponent.astro';
import ServiceHero from '#storyblok/modules/service/ServiceHero.astro';

type Props = { blok: StoryblokService };

const { blok } = Astro.props;
---

<ServiceHero blok={blok.informations[0] as StoryblokServiceInformations} />

{
	blok.body?.map((blok, i) => {
		return <StoryblokComponent blok={blok as SbBlokData} id={blok._uid} data-component-name={blok.component} />;
	})
}
